### Performance enhancements

- Spatial predicates involving large relations (e.g. country boundaries) are now up to 30x faster

- Replaced `std` containers with Abseil-derived `HashMap` / `HashSet`

## Bug fixes

- Fixed: `contains()` returns false positives if any of the feature's tiles are missing (#43)

- Fixed FeatureStore locking behavior




